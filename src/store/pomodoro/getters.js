export default {
    timerSettings(state) {
        return state.timerSettings;
    },
    sessionTimeLeft(state) {
        return state.sessionTimeLeft;
    },
    breakTimeLeft(state) {
        return state.breakTimeLeft;
    },
    sessionsToLongBreak(state) {
        return state.sessionsToLongBreak;
    }
}